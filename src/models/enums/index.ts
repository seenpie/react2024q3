export enum AppRoutes {
  HOME_ROUTE = "/",
  FORM_UNCONTROLLED_ROUTE = "/uncontrolled-form",
  FORM_HOOK_ROUTE = "/hook-form"
}

export enum Genders {
  MALE = "male",
  FEMALE = "female"
}

export enum FormType {
  HOOK_FORM = "hook form",
  UNCONTROLLED_FORM = "uncontrolled form"
}
